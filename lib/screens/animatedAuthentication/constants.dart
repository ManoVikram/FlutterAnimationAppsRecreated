import 'package:flutter/material.dart';

const Color bgLogin = Color(0xFF00C470);
const Color bgSignup = Color(0xFF000A54);

const double defaultPadding = 16.0;
const Duration defaultDuration = Duration(milliseconds: 300);
