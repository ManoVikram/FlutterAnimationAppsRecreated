import 'package:flutter/material.dart';

class AnimatedLoadingHomeScreen extends StatelessWidget {
  const AnimatedLoadingHomeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
